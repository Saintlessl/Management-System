import { useQuery } from '@tanstack/react-query';
import { operationsApi } from '@/api/operations';

export function AuditLogsPage() {
  const { data, isLoading, isError } = useQuery({ queryKey: ['audit-logs'], queryFn: operationsApi.auditLogs });
  return <div className="space-y-5"><div><h1 className="text-2xl font-bold">Audit Logs</h1><p className="mt-1 text-sm text-slate-500">Riwayat terstruktur untuk perubahan penting.</p></div><div className="overflow-x-auto rounded-xl border border-slate-200 bg-white"><table className="w-full min-w-200 text-left text-sm"><thead className="bg-slate-50"><tr><th className="px-4 py-3">Waktu</th><th className="px-4 py-3">Actor</th><th className="px-4 py-3">Action</th><th className="px-4 py-3">Entity</th><th className="px-4 py-3">Changes</th></tr></thead><tbody className="divide-y divide-slate-100">{isLoading ? <tr><td colSpan={5} className="p-10 text-center">Memuat audit...</td></tr> : isError ? <tr><td colSpan={5} className="p-10 text-center text-red-600">Audit gagal dimuat.</td></tr> : data?.data.map((item) => <tr key={item.id}><td className="px-4 py-3 text-xs text-slate-500">{item.created_at}</td><td className="px-4 py-3">{item.user?.name ?? 'System'}</td><td className="px-4 py-3 font-medium">{item.action}</td><td className="px-4 py-3">{item.entity_type} #{item.entity_id}</td><td className="px-4 py-3"><details><summary className="cursor-pointer text-blue-600">View JSON</summary><pre className="mt-2 max-w-md overflow-x-auto rounded bg-slate-900 p-2 text-xs text-white">{JSON.stringify({ old: item.old_value, new: item.new_value }, null, 2)}</pre></details></td></tr>)}</tbody></table></div></div>;
}
